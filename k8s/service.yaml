apiVersion: v1
kind: Service
metadata:
  name: jlpt-helper-service
  labels:
    app: jlpt-helper
spec:
  type: ClusterIP  # LoadBalancer 또는 NodePort로 변경 가능
  selector:
    app: jlpt-helper
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
